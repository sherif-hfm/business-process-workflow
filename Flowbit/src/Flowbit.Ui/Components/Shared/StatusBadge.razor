<span class="status-badge status-@Tone">
    <span class="status-dot" aria-hidden="true"></span>
    @Label
</span>

@code {
    [Parameter, EditorRequired] public string? Status { get; set; }
    [Parameter] public string? Text { get; set; }

    private string Normalized => Status?.Trim().ToLowerInvariant() ?? string.Empty;
    private string Label => Text ?? (string.IsNullOrWhiteSpace(Status)
        ? "Unknown"
        : char.ToUpperInvariant(Status![0]) + Status[1..]);
    private string Tone => Normalized switch
    {
        "running" or "active" or "available" or "published" or "ready" or "ready to act" or "eligible" or "succeeded" => "success",
        "pending" or "queued" or "retry" or "leased" or "resultready" or "can claim" or "unpublished" or "warning" or "interrupted" or "preparing" or "ineligible" or "skipped" or "completedwithissues" => "warning",
        "open" or "incident" or "faulted" or "failed" or "error" or "cancelled" or "danger" => "danger",
        "completed" or "resolved" or "merged" or "default" or "assigned" or "primary" => "primary",
        _ => "neutral"
    };
}
